CELERY_WORKER_CONCURRENCY=4

# Root for the per-task Okapy extraction workspaces. Kept on the shared data
# volume because a workspace holds a full study and several run concurrently;
# the container's own /tmp would put that on its writable layer.
QUANTIMAGE_WORK_DIR=/quantimage2-data/tmp/extraction
